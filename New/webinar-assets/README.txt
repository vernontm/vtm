Webinar presenter photos for /webinar.

Drop two headshots here with these exact names:
  rayvaughn.png   (Rayvaughn Vernon)
  nesha.png       (Nesha Danay)

Recommended: portrait crop, roughly 4:5 (e.g. 800x1000), face centered near the
top. JPG is fine too — if you use .jpg, update the two <img src> paths in
New/webinar.html (search for /webinar-assets/).

Until these files exist, the page shows a gold "RV" / "ND" placeholder frame
labeled "PHOTO COMING", so the page is safe to publish either way.
