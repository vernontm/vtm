# Engineer queue

Tasks for the scheduled VTM engineer agent. One bullet per task, newest at the bottom. The agent works the TOP unchecked item each run, marks it `[PR]` with a link when a PR is open, and checks it off only after the PR merges.

Rules: no secrets or customer data in this file. Business decisions get a written question in LOG.md instead of an implementation.

## Queue

- [ ] (Ray is filing the first issue via Slack; meeting triage or the heartbeat routes it here. Do not invent work in the meantime; an empty run is a good run.)
